# Output audit & telemetri lokal — JANGAN commit (berisi konten prompt/command, PII)
data/

# Backup settings yang dibuat installer
*.bak-*

# Output generate (managed-settings spesifik lingkungan)
dist/

# Node / OS
node_modules/
*.log
.DS_Store
Thumbs.db

# Secrets lokal (jangan pernah commit token SIEM dll.)
.env
*.local
secrets.*
